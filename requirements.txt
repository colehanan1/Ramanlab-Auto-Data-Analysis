
# Core scientific stack
# Reaction prediction artifacts were trained on NumPy 1.x; keep the runtime below 2.0
# so the bundled models load without re-exporting them.
numpy>=1.24,<2
pandas>=2.0
matplotlib>=3.7
scipy>=1.11
scikit-image>=0.22
tqdm>=4.66
PyYAML>=6.0.1
python-dotenv>=1.0.1

# CV / DL
opencv-python>=4.9
ultralytics>=8.3.0,<9

# Video composition (2.x removed moviepy.editor; keep 1.x for backwards compatibility)
moviepy>=1.0.3,<2
Pillow>=10.0

pandas-stubs==2.2.3.250308
types-pytz>=2022.1.1

flybehavior-response @ git+https://github.com/colehanan1/FlyBehaviorScoring.git
